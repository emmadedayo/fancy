enum Gender {
  Male = 'male',
  Female = 'female',
  NonBinary = 'non-binary',
  PreferNotToSay = 'prefer not to say',
  Custom = 'custom', // Allow users to specify a custom gender
}

export default Gender; // this makes it reusable
